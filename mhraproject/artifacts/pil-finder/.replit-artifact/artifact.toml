kind = "web"
previewPath = "/"
title = "MHRA PIL Finder"
version = "1.0.0"
id = "artifacts/pil-finder"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/pil-finder run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pil-finder", "run", "build" ]
publicDir = "artifacts/pil-finder/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
API_PORT = "8080"
